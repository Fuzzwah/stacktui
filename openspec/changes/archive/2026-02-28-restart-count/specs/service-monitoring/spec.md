### Delta: Restart Count Badge

ServicePanel status line gains restart count badge after existing indicators. Format: `↻N` in yellow (1-4) or red (5+). Only shown when count > 0. Services with 5+ restarts are auto-selected.
